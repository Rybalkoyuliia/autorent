import styled from 'styled-components';

export const StyledCatalogContainer = styled.div`
  overflow-y: scroll;
  overflow-x: hidden;
`;

export const StyledMainContainerMain = styled.main`
  margin: 150px 40px;
  height: 100vh;
`;
